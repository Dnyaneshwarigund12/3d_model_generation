# GPU-side dependencies, installed inside the Colab notebook on top of
# requirements.txt. Colab already ships a working CUDA torch build, so torch is
# deliberately NOT pinned here - reinstalling it is the most common way to break
# a Colab session.

# Shared by the generation models
huggingface-hub>=0.23
safetensors>=0.4
transformers>=4.40
diffusers>=0.30
accelerate>=0.30
omegaconf>=2.3
einops>=0.7
imageio>=2.31

# TripoSR: mesh extraction + texture export
torchmcubes @ git+https://github.com/tatsy/torchmcubes.git
xatlas>=0.0.9
pygltflib>=1.16

# Hunyuan3D 2.1 (quality path) additionally needs custom CUDA extensions that
# must be compiled from its own repo - the notebook does this.

# Monocular metric depth (Tier 3 fallback). UniDepth is installed from source in
# the notebook because it needs its own CUDA ops; Metric3D loads via torch.hub
# and needs nothing extra beyond timm.
timm>=0.9.0
