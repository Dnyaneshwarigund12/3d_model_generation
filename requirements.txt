# Core pipeline dependencies (CPU-safe, no CUDA needed). Enough to run the whole
# measurement pipeline, the test suite and the UI with the `silhouette` backend.
#
# On Colab, do NOT install these by hand: run tools/colab_setup.py, which pins the
# versions that image already has before installing anything, so no resolution can
# move numpy or torch underneath the rest of the stack. It handles the generation
# backends too.

numpy>=1.24,<3
opencv-python>=4.8
pillow>=10.0
trimesh>=4.0
scipy>=1.10

# Background removal (runs on CPU via onnxruntime)
rembg>=2.0.50
onnxruntime>=1.16

# Web UI
gradio>=4.44

# Tests
pytest>=8.0
